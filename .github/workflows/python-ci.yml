name: CI - Django Polls App

on:
  push:
  pull_request:

jobs:
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest

    outputs:
      build-ok: true

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Installer dépendances Python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Vérification versions
        run: |
          python --version
          pip list

  ruff:
    name: 🌪 Ruff
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Installer Ruff
        run: |
          pip install ruff

      - name: Lancer Ruff
        run: |
          echo "Lancement de Ruff..."
          ruff check .

  pylint:
    name: 🔍 Pylint (min 7/10)
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Installer dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lancer Pylint + extraction score
        run: |
          echo "Exécution de Pylint..."
          pylint *.py > pylint_output.txt || true

          echo "==== Résultat Pylint ===="
          cat pylint_output.txt

          SCORE=$(grep -oP "(?<=rated at )[\d\.]+" pylint_output.txt || echo "0")
          echo "Score pylint détecté : $SCORE / 10"

          python3 - <<EOF
          score = float("$SCORE")
          if score < 7:
              print("❌ Score Pylint insuffisant (<7/10). Échec du job.")
              exit(1)
          else:
              print("✅ Score Pylint suffisant.")
          EOF

  tests:
    name: 🧪 Tests Django
    runs-on: ubuntu-latest
    needs:
      - build

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Installer dépendances
        run: |
          pip install -r requirements.txt

      - name: Lancer tests Django
        run: |
          python manage.py migrate --noinput
          python manage.py test

  docker:
    name: 🐳 Docker Build
    runs-on: ubuntu-latest
    needs: tests

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t django-polls:latest .
